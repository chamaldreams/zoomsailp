 <nav class="navbar navbar-inverse navbar-static-top">
      <div class="container">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
          <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar-collapse-1">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
         <%if !current_user%> 
          <%= link_to 'ZoomSails', root_path, class: 'navbar-brand' %>
         <% elsif current_user.has_role? :guest%>
          <%= link_to 'ZoomSails', home_guest_path, class: 'navbar-brand' %>
         <% elsif current_user.has_role? :admin%> 
          <%= link_to 'ZoomSails', home_admin_path, class: 'navbar-brand' %>
         <% elsif current_user.has_role? :customer%> 
          <%= link_to 'ZoomSails', home_customer_path, class: 'navbar-brand' %>
         <% elsif current_user.has_role? :dealer%> 
          <%= link_to 'ZoomSails', home_dealer_path, class: 'navbar-brand' %>
         <% elsif current_user.has_role? :prodution%> 
          <%= link_to 'ZoomSails', home_production_path, class: 'navbar-brand' %> 
          <%end%>
        </div>
        
        
<div class="collapse navbar-collapse pull-right">
          <ul class="nav navbar-nav">
          <% if user_signed_in? %> 
 

         <li><%= link_to "#{current_user.email.downcase} #{current_user.roles_name}", edit_user_registration_path %></li>
           <li class="round-image-50"><%= image_tag(current_user.avatar.url(:thumb)) %></li>

          <li><%= link_to 'Log Out',destroy_user_session_path, method: :delete%></li>
            
           
<% if current_user.has_role? :admin or current_user.has_role? :super_admin %>

         <li class="dropdown"><a class="dropdown-toggle" data-toggle="dropdown" href="#">Settings <span class="caret"></span></a>
        <ul class="dropdown-menu">
          <li><%= link_to 'User Management', users_path%></li>
          <li><a href="#">Page 1-2</a></li>
          <li><a href="#">Page 1-3</a></li>
        </ul>
        </li>    









</ul>
</div>
</div>
<% end %>
<% end %>















        </nav>